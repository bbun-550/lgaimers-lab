# Experiments Configuration

This directory stores Hydra configuration files for specific experiments.

## Usage
- Run an experiment by name:
  ```bash
  make train experiment=experiment_name
  ```
- Configs here override the defaults in `../config.yaml`.
